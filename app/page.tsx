"use client";

import { useState, type FormEvent } from "react";
import {
  ArrowRight, CalendarDays, Check, ChevronDown, FileCheck2, LayoutDashboard,
  LockKeyhole, LogOut, Menu, Package, Search, ShieldCheck, UserPlus, Users, X,
} from "lucide-react";

type Role = "Personnel" | "SDM" | "ADJT";
type Squadron = "Agni" | "Bharat" | "Chardikala" | "Jhangi";
type LeaveType = "AL" | "CL";
type Status = "Pending" | "Approved" | "Rejected";

type Account = { armyNo: string; password: string; name: string; role: Role; squadron?: Squadron };
type LeaveRequest = {
  id: number; armyNo: string; name: string; squadron: Squadron; type: LeaveType;
  from: string; to: string; prefix: string; suffix: string; reportingDate: string; status: Status;
};

const squadrons: Squadron[] = ["Agni", "Bharat", "Chardikala", "Jhangi"];
const squadronData: Record<Squadron, { strength: number; available: number; leave: number; course: number; color: string }> = {
  Agni: { strength: 48, available: 44, leave: 3, course: 1, color: "#b96f49" },
  Bharat: { strength: 46, available: 43, leave: 2, course: 1, color: "#4b8b6d" },
  Chardikala: { strength: 47, available: 42, leave: 3, course: 2, color: "#6d7fa2" },
  Jhangi: { strength: 46, available: 43, leave: 2, course: 1, color: "#9a769c" },
};
const seedAccounts: Account[] = [
  { armyNo: "630001", password: "personnel", name: "Arjun Kumar", role: "Personnel", squadron: "Agni" },
  { armyNo: "SDM-AGNI", password: "sdm123", name: "Major Vikram Singh", role: "SDM", squadron: "Agni" },
  { armyNo: "SDM-BHARAT", password: "sdm123", name: "Major Rakesh Pradhan", role: "SDM", squadron: "Bharat" },
  { armyNo: "SDM-CHARDIKALA", password: "sdm123", name: "Major Sanjay Bhati", role: "SDM", squadron: "Chardikala" },
  { armyNo: "SDM-JHANGI", password: "sdm123", name: "Major Karan Yadav", role: "SDM", squadron: "Jhangi" },
  { armyNo: "ADJT-001", password: "adjt123", name: "Lt Col Naveen Rathore", role: "ADJT" },
];
const seedRequests: LeaveRequest[] = [
  { id: 1, armyNo: "630014", name: "A•••• S••••", squadron: "Agni", type: "AL", from: "2026-08-25", to: "2026-08-29", prefix: "2026-08-24", suffix: "2026-08-30", reportingDate: "2026-08-31", status: "Pending" },
  { id: 2, armyNo: "630087", name: "V•••• C••••", squadron: "Agni", type: "CL", from: "2026-08-22", to: "2026-08-23", prefix: "None", suffix: "None", reportingDate: "2026-08-24", status: "Approved" },
  { id: 3, armyNo: "630126", name: "R•••• P••••", squadron: "Bharat", type: "AL", from: "2026-08-28", to: "2026-09-02", prefix: "2026-08-27", suffix: "None", reportingDate: "2026-09-03", status: "Pending" },
  { id: 4, armyNo: "630211", name: "S•••• B••••", squadron: "Chardikala", type: "CL", from: "2026-08-21", to: "2026-08-22", prefix: "None", suffix: "None", reportingDate: "2026-08-23", status: "Pending" },
];

export default function Home() {
  const [accounts, setAccounts] = useState(seedAccounts);
  const [session, setSession] = useState<Account | null>(null);
  const [requests, setRequests] = useState(seedRequests);
  const [authMode, setAuthMode] = useState<"login" | "register">("login");
  if (!session) return <AuthScreen accounts={accounts} mode={authMode} setMode={setAuthMode} onLogin={setSession} onRegister={(account) => { setAccounts((items) => [...items, account]); setSession(account); }} />;
  return <AppShell account={session} requests={requests} setRequests={setRequests} onLogout={() => setSession(null)} />;
}

function AuthScreen({ accounts, mode, setMode, onLogin, onRegister }: { accounts: Account[]; mode: "login" | "register"; setMode: (mode: "login" | "register") => void; onLogin: (account: Account) => void; onRegister: (account: Account) => void }) {
  const [armyNo, setArmyNo] = useState(""); const [password, setPassword] = useState(""); const [name, setName] = useState("");
  const [role, setRole] = useState<Role>("Personnel"); const [squadron, setSquadron] = useState<Squadron>("Agni"); const [error, setError] = useState("");
  function submit(event: FormEvent<HTMLFormElement>) {
    event.preventDefault(); const normalized = armyNo.trim().toUpperCase();
    if (mode === "login") { const account = accounts.find((item) => item.armyNo.toUpperCase() === normalized && item.password === password); if (!account) { setError("Army No. or password is incorrect."); return; } onLogin(account); return; }
    if (!name.trim() || normalized.length < 4 || password.length < 6) { setError("Enter a name, valid Army No., and password of 6 or more characters."); return; }
    if (accounts.some((item) => item.armyNo.toUpperCase() === normalized)) { setError("That Army No. already has a profile. Please sign in."); return; }
    onRegister({ armyNo: normalized, password, name: name.trim(), role, squadron: role === "ADJT" ? undefined : squadron });
  }
  return <main className="relative flex min-h-screen items-center overflow-hidden bg-[#edf4ef] px-5 py-10 text-[#193c30] sm:px-8"><div className="absolute inset-0 bg-[radial-gradient(circle_at_12%_12%,rgba(211,233,219,0.9),transparent_30%),radial-gradient(circle_at_90%_85%,rgba(245,222,193,0.58),transparent_28%)]" /><div className="relative mx-auto grid w-full max-w-5xl overflow-hidden rounded-[28px] border border-white/70 bg-white/65 shadow-[0_30px_100px_rgba(37,75,56,0.14)] backdrop-blur-xl lg:grid-cols-[0.92fr_1.08fr]"><section className="relative flex min-h-[620px] flex-col justify-between overflow-hidden bg-[#183f31] p-7 text-white sm:p-10"><div className="absolute -right-20 -top-20 h-64 w-64 rounded-full border-[38px] border-[#2c624b]" /><div className="relative"><Brand light /><div className="mb-3 mt-16 flex items-center gap-2 text-[10px] font-bold uppercase tracking-[0.18em] text-[#a7cbb7]"><span className="h-1.5 w-1.5 rounded-full bg-[#79bd91]" /> Secure personnel command</div><h1 className="text-4xl font-bold leading-[1.02] tracking-[-0.06em] sm:text-5xl">One unit.<br /><span className="text-[#b9dbc7]">One picture.</span></h1><p className="mt-5 max-w-xs text-sm leading-6 text-[#b7d0c1]">A protected operational workspace for personnel, Squadron SDMs, and the ADJT.</p></div><div className="relative border-t border-[#396650] pt-5 text-[11px] text-[#a9c9b7]"><div className="flex items-center gap-2"><LockKeyhole size={13} /> Access is logged and identity data is masked by default.</div></div></section><section className="bg-[#fbfdfb]/90 p-7 sm:p-10"><div className="mb-8 flex items-start justify-between"><div><div className="mb-1 text-[10px] font-bold uppercase tracking-[0.18em] text-[#86a096]">{mode === "login" ? "Secure sign in" : "New profile"}</div><h2 className="text-2xl font-bold tracking-[-0.05em] text-[#204736]">{mode === "login" ? "Welcome back" : "Create your profile"}</h2></div><div className="rounded-xl bg-[#e6f2e9] p-3 text-[#3f8662]"><UserPlus size={19} /></div></div><form onSubmit={submit} className="space-y-4"><Field label="Army No." value={armyNo} setValue={setArmyNo} placeholder="e.g. 630001" autoComplete="username" /><Field label="Password" value={password} setValue={setPassword} placeholder="Enter password" type="password" autoComplete={mode === "login" ? "current-password" : "new-password"} />{mode === "register" && <><Field label="Full name" value={name} setValue={setName} placeholder="Service member name" /><SelectField label="Profile type" value={role} setValue={(value) => setRole(value as Role)} options={["Personnel", "SDM", "ADJT"]} />{role !== "ADJT" && <SelectField label="Squadron" value={squadron} setValue={(value) => setSquadron(value as Squadron)} options={squadrons} />}</>}{error && <p className="rounded-lg bg-[#fbedeb] px-3 py-2.5 text-xs font-semibold text-[#a8564b]">{error}</p>}<button className="flex w-full items-center justify-center gap-2 rounded-xl bg-[#1d513c] py-3.5 text-sm font-bold text-white shadow-lg shadow-[#1d513c]/15 transition hover:bg-[#286e50]">{mode === "login" ? "Sign in securely" : "Create profile"}<ArrowRight size={16} /></button></form><div className="mt-7 border-t border-[#e6eee8] pt-5 text-center text-xs text-[#82958a]">{mode === "login" ? "First time using Tresath?" : "Already have a Tresath profile?"}<button onClick={() => { setMode(mode === "login" ? "register" : "login"); setError(""); }} className="ml-1 font-bold text-[#39805e] hover:underline">{mode === "login" ? "Create one" : "Sign in"}</button></div><p className="mt-8 text-center text-[10px] leading-5 text-[#a0aea6]">Demo: <b>630001 / personnel</b> · <b>SDM-AGNI / sdm123</b> · <b>ADJT-001 / adjt123</b></p></section></div></main>;
}

function AppShell({ account, requests, setRequests, onLogout }: { account: Account; requests: LeaveRequest[]; setRequests: (requests: LeaveRequest[]) => void; onLogout: () => void }) {
  const [active, setActive] = useState("Overview"); const [menuOpen, setMenuOpen] = useState(false);
  const nav = account.role === "Personnel" ? ["Overview", "My leave", "Material"] : ["Overview", "Leave board", "Nominal roll", "Material"];
  return <div className="min-h-screen bg-[#f1f5f2] text-[#1b3027]"><aside className={`fixed inset-y-0 left-0 z-40 flex w-[258px] flex-col border-r border-[#d5e1d9] bg-[#f8faf8]/95 px-5 py-6 backdrop-blur-xl transition-transform lg:translate-x-0 ${menuOpen ? "translate-x-0" : "-translate-x-full"}`}><div className="mb-12 flex items-center justify-between px-2"><Brand /><button aria-label="Close menu" onClick={() => setMenuOpen(false)} className="text-[#789087] lg:hidden"><X size={20} /></button></div><div className="mb-3 px-3 text-[10px] font-bold uppercase tracking-[0.18em] text-[#9aaba2]">{account.role} workspace</div><nav className="space-y-1">{nav.map((item, index) => <button key={item} onClick={() => { setActive(item); setMenuOpen(false); }} className={`flex w-full items-center gap-3 rounded-xl px-3 py-3 text-left text-[13px] font-semibold ${active === item ? "bg-[#e0eee6] text-[#246147]" : "text-[#71847a] hover:bg-[#edf3ef]"}`}>{index === 0 ? <LayoutDashboard size={17} /> : item === "Material" ? <Package size={17} /> : item === "Nominal roll" ? <Users size={17} /> : <CalendarDays size={17} />}<span className="flex-1">{item}</span>{item === "Leave board" && <span className="rounded-md bg-[#f5e3d8] px-1.5 py-0.5 text-[10px] text-[#a76142]">03</span>}</button>)}</nav><div className="mt-auto rounded-2xl border border-[#d7e3db] bg-white/65 p-4"><div className="mb-3 flex items-center gap-2 text-[11px] font-bold text-[#587268]"><LockKeyhole size={14} /> Secure workspace</div><p className="text-[11px] leading-5 text-[#8b9b92]">Identity data is masked. Access is logged under unit privacy controls.</p></div></aside>{menuOpen && <button aria-label="Close sidebar" onClick={() => setMenuOpen(false)} className="fixed inset-0 z-30 bg-[#173b2e]/20 lg:hidden" />}<main className="min-h-screen lg:ml-[258px]"><header className="flex h-[78px] items-center justify-between border-b border-[#dce6df] bg-[#f7faf7]/75 px-5 backdrop-blur-xl sm:px-8 lg:px-10"><div className="flex items-center gap-3"><button aria-label="Open menu" onClick={() => setMenuOpen(true)} className="text-[#315d4b] lg:hidden"><Menu size={22} /></button><span className="text-xs text-[#899a91]">63 Cavalry <span className="mx-2">/</span> <strong className="text-[#385a4b]">{active}</strong></span></div><div className="flex items-center gap-3"><div className="hidden text-right sm:block"><div className="text-xs font-bold text-[#2d5141]">{account.name}</div><div className="text-[10px] text-[#8a9b92]">{account.armyNo} · {account.role}{account.squadron ? ` · ${account.squadron} Sqn` : ""}</div></div><div className="flex h-8 w-8 items-center justify-center rounded-full bg-[#c8ddd1] text-[10px] font-bold text-[#285d49]">{account.name.split(" ").map((part) => part[0]).slice(0, 2).join("")}</div><button aria-label="Sign out" onClick={onLogout} className="rounded-lg p-2 text-[#789087] hover:bg-white"><LogOut size={16} /></button></div></header><div className="mx-auto max-w-[1450px] px-5 py-7 sm:px-8 lg:px-10 lg:py-9">{account.role === "Personnel" ? <PersonnelView account={account} requests={requests} setRequests={setRequests} /> : account.role === "SDM" ? <SdmView account={account} requests={requests} setRequests={setRequests} /> : <AdjtView requests={requests} setRequests={setRequests} />}</div></main></div>;
}

function PersonnelView({ account, requests, setRequests }: { account: Account; requests: LeaveRequest[]; setRequests: (requests: LeaveRequest[]) => void }) {
  const [leaveType, setLeaveType] = useState<LeaveType>("AL"); const [submitted, setSubmitted] = useState(false);
  const [form, setForm] = useState({ from: "", to: "", prefix: "", suffix: "", reportingDate: "", days: 1 }); const balance = leaveType === "AL" ? 60 : 30;
  const ownRequests = requests.filter((request) => request.armyNo === account.armyNo);
  const update = (key: keyof typeof form, value: string | number) => setForm((current) => ({ ...current, [key]: value }));
  function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); if (!account.squadron || form.days < 1 || form.days > balance || !form.from || !form.to || !form.reportingDate) return; setRequests([...requests, { id: Date.now(), armyNo: account.armyNo, name: account.name.replace(/(\w)\w+/g, "$1••••"), squadron: account.squadron, type: leaveType, from: form.from, to: form.to, prefix: form.prefix || "None", suffix: form.suffix || "None", reportingDate: form.reportingDate, status: "Pending" }]); setSubmitted(true); }
  return <><PageHeading eyebrow={`${account.squadron ?? "Unit"} Squadron · Individual personnel`} title={`Good morning, ${account.name.split(" ")[0]}`} description="Submit leave details and follow both signature decisions from one place." /><div className="grid gap-6 xl:grid-cols-[1.2fr_0.8fr]"><section className="rounded-2xl border border-[#d8e4dc] bg-white/80 p-5 shadow-sm sm:p-6"><SectionTitle eyebrow="Leave application" title="Request leave" icon={CalendarDays} /><div className="mb-5 grid grid-cols-2 gap-2"><LeaveType type="AL" selected={leaveType === "AL"} onClick={() => setLeaveType("AL")} days={60} /><LeaveType type="CL" selected={leaveType === "CL"} onClick={() => setLeaveType("CL")} days={30} /></div>{submitted && <div className="mb-4 rounded-xl bg-[#e4f2e9] p-3 text-xs font-bold text-[#438064]">Request submitted to {account.squadron} SDM for review.</div>}<form onSubmit={submit} className="grid gap-4 sm:grid-cols-2">{(["from", "to", "prefix", "suffix", "reportingDate"] as const).map((key) => <DateField key={key} label={key === "reportingDate" ? "Reporting date" : key[0].toUpperCase() + key.slice(1)} value={form[key]} required={key === "from" || key === "to" || key === "reportingDate"} onChange={(value) => update(key, value)} />)}<label className="text-[10px] font-bold uppercase tracking-wider text-[#83988b]">Days<input type="number" min={1} max={balance} value={form.days} onChange={(event) => update("days", Number(event.target.value))} className="mt-1.5 w-full rounded-lg border border-[#dce7df] bg-white px-3 py-2.5 text-sm font-bold text-[#345a49] outline-none focus:border-[#75ad8c]" /></label><div className="sm:col-span-2 flex flex-wrap items-center justify-between gap-3 border-t border-[#edf1ed] pt-4"><span className="text-xs text-[#84958c]">Remaining {leaveType}: <strong className="text-[#47705d]">{Math.max(0, balance - form.days)} days</strong></span><button className="flex items-center gap-2 rounded-xl bg-[#1c503c] px-5 py-3 text-xs font-bold text-white hover:bg-[#286b50]"><FileCheck2 size={15} /> Send to SDM</button></div></form></section><section className="rounded-2xl border border-[#d8e4dc] bg-white/80 p-5 shadow-sm sm:p-6"><SectionTitle eyebrow="Allowance" title="Leave balance" icon={CalendarDays} /><div className="grid grid-cols-2 gap-3"><Balance label="Annual leave" value={60} /><Balance label="Casual leave" value={30} /></div><div className="mt-6 border-t border-[#edf1ed] pt-5"><div className="mb-4 text-[10px] font-bold uppercase tracking-wider text-[#91a198]">Your approval trail</div>{ownRequests.length ? ownRequests.map((request) => <RequestRow key={request.id} request={request} />) : <p className="text-xs text-[#8a9b92]">No submitted requests yet.</p>}</div></section></div></>;
}

function SdmView({ account, requests, setRequests }: { account: Account; requests: LeaveRequest[]; setRequests: (requests: LeaveRequest[]) => void }) {
  const squadron = account.squadron as Squadron; const data = squadronData[squadron]; const [query, setQuery] = useState("");
  const filtered = requests.filter((request) => request.squadron === squadron && `${request.name} ${request.armyNo}`.toLowerCase().includes(query.toLowerCase()));
  return <><PageHeading eyebrow={`${squadron} Squadron · Squadron Duty Manager`} title={`${squadron} leave board`} description="Review personnel leave, reporting dates, and forward valid requests to the ADJT." /><MetricGrid data={data} /><section className="mt-6 overflow-hidden rounded-2xl border border-[#d8e4dc] bg-white/80 shadow-sm"><div className="flex flex-col gap-4 border-b border-[#edf1ed] p-5 sm:flex-row sm:items-center sm:justify-between sm:p-6"><SectionTitle eyebrow="Squadron register" title="Leave board" icon={Users} /><label className="flex h-9 items-center gap-2 rounded-lg border border-[#dbe6df] bg-[#fbfdfb] px-3 text-xs text-[#8a9b92] sm:w-60"><Search size={15} /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search Army No..." className="w-full bg-transparent outline-none" /></label></div><div className="divide-y divide-[#eff3ef]">{filtered.map((request) => <LeaveBoardRow key={request.id} request={request} onDecision={(status) => setRequests(requests.map((item) => item.id === request.id ? { ...item, status } : item))} />)}{filtered.length === 0 && <p className="p-8 text-center text-xs text-[#8a9b92]">No leave entries match this squadron.</p>}</div></section></>;
}

function AdjtView({ requests, setRequests }: { requests: LeaveRequest[]; setRequests: (requests: LeaveRequest[]) => void }) {
  const [query, setQuery] = useState(""); const filtered = requests.filter((request) => `${request.name} ${request.armyNo} ${request.squadron}`.toLowerCase().includes(query.toLowerCase()));
  return <><PageHeading eyebrow="Adjutant branch · All squadrons" title="Unit readiness board" description="All four squadron strength figures, leave boards, and reporting commitments in one command view." /><div className="mb-6 grid gap-3 sm:grid-cols-2 xl:grid-cols-4">{squadrons.map((squadron) => <div key={squadron} className="rounded-2xl border border-[#d8e4dc] bg-white/80 p-5 shadow-sm"><div className="flex items-center justify-between"><span className="text-xs font-bold text-[#446555]">{squadron} Sqn</span><span className="h-2 w-2 rounded-full" style={{ backgroundColor: squadronData[squadron].color }} /></div><div className="mt-4 text-2xl font-bold tracking-[-0.05em] text-[#274c3c]">{squadronData[squadron].strength}</div><div className="mt-1 text-[10px] text-[#91a198]">strength · {squadronData[squadron].available} available</div><div className="mt-4 flex gap-3 border-t border-[#edf1ed] pt-3 text-[10px] text-[#83958b]"><span>{squadronData[squadron].leave} leave</span><span>{squadronData[squadron].course} course / TDY</span></div></div>)}</div><section className="overflow-hidden rounded-2xl border border-[#d8e4dc] bg-white/80 shadow-sm"><div className="flex flex-col gap-4 border-b border-[#edf1ed] p-5 sm:flex-row sm:items-center sm:justify-between sm:p-6"><SectionTitle eyebrow="ADJT decision queue" title="All squadron leave board" icon={LayoutDashboard} /><label className="flex h-9 items-center gap-2 rounded-lg border border-[#dbe6df] bg-[#fbfdfb] px-3 text-xs text-[#8a9b92] sm:w-64"><Search size={15} /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search all squadrons..." className="w-full bg-transparent outline-none" /></label></div><div className="divide-y divide-[#eff3ef]">{filtered.map((request) => <LeaveBoardRow key={request.id} request={request} onDecision={(status) => setRequests(requests.map((item) => item.id === request.id ? { ...item, status } : item))} />)}</div></section></>;
}

function Brand({ light = false }: { light?: boolean }) { return <div className="flex items-center gap-3"><div className={`flex h-10 w-10 items-center justify-center rounded-xl ${light ? "bg-[#d9ebe0] text-[#214f3b]" : "bg-[#173b2e] text-[#dceee3]"}`}><ShieldCheck size={21} /></div><div><div className={`text-lg font-bold tracking-[-0.05em] ${light ? "text-white" : "text-[#173b2e]"}`}>tresath</div><div className={`text-[9px] font-bold uppercase tracking-[0.2em] ${light ? "text-[#a8cbb8]" : "text-[#7e9287]"}`}>63 Cavalry</div></div></div>; }
function PageHeading({ eyebrow, title, description }: { eyebrow: string; title: string; description: string }) { return <section className="mb-7"><div className="mb-2 text-[10px] font-bold uppercase tracking-[0.18em] text-[#739183]">{eyebrow}</div><h1 className="text-[clamp(1.8rem,4vw,2.6rem)] font-bold tracking-[-0.055em] text-[#193c30]">{title}</h1><p className="mt-1 text-sm text-[#81938a]">{description}</p></section>; }
function SectionTitle({ eyebrow, title, icon: Icon }: { eyebrow: string; title: string; icon: typeof CalendarDays }) { return <div><div className="mb-1 flex items-center gap-2 text-[10px] font-bold uppercase tracking-[0.17em] text-[#88a095]"><Icon size={13} />{eyebrow}</div><h2 className="text-base font-bold text-[#244b3b]">{title}</h2></div>; }
function Field({ label, value, setValue, placeholder, type = "text", autoComplete }: { label: string; value: string; setValue: (value: string) => void; placeholder: string; type?: string; autoComplete?: string }) { return <label className="block text-[10px] font-bold uppercase tracking-wider text-[#83988b]">{label}<input type={type} value={value} onChange={(event) => setValue(event.target.value)} placeholder={placeholder} autoComplete={autoComplete} className="mt-1.5 w-full rounded-xl border border-[#dce7df] bg-white px-3.5 py-3 text-sm text-[#345a49] outline-none placeholder:text-[#a6b2ac] focus:border-[#75ad8c]" /></label>; }
function SelectField({ label, value, setValue, options }: { label: string; value: string; setValue: (value: string) => void; options: string[] }) { return <label className="block text-[10px] font-bold uppercase tracking-wider text-[#83988b]">{label}<span className="relative mt-1.5 block"><select value={value} onChange={(event) => setValue(event.target.value)} className="w-full appearance-none rounded-xl border border-[#dce7df] bg-white px-3.5 py-3 text-sm text-[#345a49] outline-none focus:border-[#75ad8c]">{options.map((option) => <option key={option}>{option}</option>)}</select><ChevronDown size={15} className="pointer-events-none absolute right-3 top-3.5 text-[#849b8f]" /></span></label>; }
function DateField({ label, value, onChange, required }: { label: string; value: string; onChange: (value: string) => void; required?: boolean }) { return <label className="block text-[10px] font-bold uppercase tracking-wider text-[#83988b]">{label}<input type="date" required={required} value={value} onChange={(event) => onChange(event.target.value)} className="mt-1.5 w-full rounded-lg border border-[#dce7df] bg-white px-3 py-2.5 text-xs font-semibold text-[#486958] outline-none focus:border-[#75ad8c]" /></label>; }
function LeaveType({ type, days, selected, onClick }: { type: LeaveType; days: number; selected: boolean; onClick: () => void }) { return <button type="button" onClick={onClick} className={`rounded-xl p-3 text-left ${selected ? type === "AL" ? "bg-[#f7f0e5] ring-1 ring-[#e1cba8]" : "bg-[#e7f2eb] ring-1 ring-[#a9ceb6]" : "bg-[#f7faf7]"}`}><div className="text-[10px] font-bold uppercase tracking-wider text-[#81968a]">{type === "AL" ? "Annual leave" : "Casual leave"}</div><div className="mt-1 text-xl font-bold text-[#416c58]">{days} <span className="text-xs font-medium">days</span></div></button>; }
function Balance({ label, value }: { label: string; value: number }) { return <div className="rounded-xl bg-[#e7f2eb] p-4"><div className="text-[10px] font-bold uppercase tracking-wider text-[#81968a]">{label}</div><div className="mt-2 text-2xl font-bold text-[#416c58]">{value}</div><div className="text-[10px] text-[#8d9d94]">days remaining</div></div>; }
function MetricGrid({ data }: { data: { strength: number; available: number; leave: number; course: number } }) { return <div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4"><Metric label="Total strength" value={data.strength} helper="personnel on roll" /><Metric label="Available" value={data.available} helper={`${Math.round(data.available / data.strength * 100)}% available`} /><Metric label="On leave" value={data.leave} helper="approved absence" /><Metric label="On course / TDY" value={data.course} helper="temporarily committed" /></div>; }
function Metric({ label, value, helper }: { label: string; value: number; helper: string }) { return <div className="rounded-2xl border border-[#d8e4dc] bg-white/80 p-5 shadow-sm"><div className="text-[10px] font-bold uppercase tracking-[0.16em] text-[#8c9d94]">{label}</div><div className="mt-4 text-2xl font-bold tracking-[-0.05em] text-[#274c3c]">{value}</div><div className="mt-1 text-[11px] text-[#91a198]">{helper}</div></div>; }
function RequestRow({ request }: { request: LeaveRequest }) { return <div className="mb-3 rounded-xl bg-[#f6f9f6] p-3"><div className="flex items-center justify-between gap-2"><span className="text-xs font-bold text-[#526e61]">{request.type} · {request.from} to {request.to}</span><Status status={request.status} /></div><div className="mt-1 text-[10px] text-[#91a198]">Reporting {request.reportingDate} · SDM review required</div></div>; }
function LeaveBoardRow({ request, onDecision }: { request: LeaveRequest; onDecision: (status: "Approved" | "Rejected") => void }) { return <div className="flex flex-col gap-4 p-5 sm:flex-row sm:items-center sm:justify-between sm:px-6"><div className="flex items-center gap-3"><div className="flex h-9 w-9 items-center justify-center rounded-full bg-[#e2eee7] text-xs font-bold text-[#3d775a]">{request.squadron.slice(0, 2).toUpperCase()}</div><div><div className="text-xs font-bold text-[#526e61]">{request.name} <span className="font-mono text-[10px] text-[#9aa9a1]">· {request.armyNo}</span></div><div className="mt-1 text-[10px] text-[#91a198]">{request.squadron} Sqn · {request.type} · {request.from} → {request.to} · report {request.reportingDate}</div></div></div><div className="flex items-center gap-3"><Status status={request.status} />{request.status === "Pending" && <div className="flex gap-1"><button onClick={() => onDecision("Approved")} aria-label="Approve request" className="rounded-lg bg-[#e4f2e9] p-2 text-[#438064]"><Check size={14} /></button><button onClick={() => onDecision("Rejected")} aria-label="Reject request" className="rounded-lg bg-[#f8eae6] p-2 text-[#a85d4e]"><X size={14} /></button></div>}</div></div>; }
function Status({ status }: { status: Status }) { return <span className={`rounded-full px-2.5 py-1 text-[10px] font-bold ${status === "Approved" ? "bg-[#e1f0e7] text-[#438064]" : status === "Rejected" ? "bg-[#f8eae6] text-[#a85d4e]" : "bg-[#f7eee0] text-[#a2764c]"}`}>{status}</span>; }

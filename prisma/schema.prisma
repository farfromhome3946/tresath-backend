datasource db {
  provider = "mongodb"
}

generator client {
  provider = "prisma-client-js"
}

// Your MongoDB data table structure
model User {
  id    String @id @default(auto()) @map("_id") @db.ObjectId
  email String @unique
  name  String?
}

